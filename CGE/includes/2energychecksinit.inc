sets
  MOD models /SATIM, eSAGE/
;

parameters
  FinalDemand(MOD,FS,C,TC,X) Final Energy Demand for SATIM sectors in SATIM and eSAGE models

  EIntensity(MOD,FS,C,TC,X) Energy Intensity in SATIM and ESAGE models

;
